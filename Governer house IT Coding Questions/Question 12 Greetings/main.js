"use strict";
let members = [`ayesha`, `hoor`, `ali`, `sana`, `sara`];
let message = `would you like to do group studies with me?`;
for (let i = 0; i < members.length; i++) {
    console.log(members[i], (message));
}
